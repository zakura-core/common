## zakura-halo2-gadgets

### Removed

- Removed the `utilities::MulByInversePowerOfTwo` trait that 1.1.0-rc.1 added.
  The lookup range check and running-sum decomposition methods no longer
  require it and again accept every `PrimeFieldBits` field, as in 1.0.1;
  Pasta `Fp`/`Fq` keep the faster running-sum scaling
  ([#350](https://github.com/zakura-core/common/pull/350)).
