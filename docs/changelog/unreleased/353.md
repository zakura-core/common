## zakura-halo2-proofs

### Changed

- Reduced prepared `k = 11` multi-circuit proving latency on eligible pools
  with at least eight workers in `multicore` builds without `orbits`. After the
  exact advice-delta density scan, advice polynomial transforms now overlap the
  remaining work-profile check, delta construction, and commitment evaluation.
  Proof format, RNG consumption, and transcript ordering are unchanged
  ([#353](https://github.com/zakura-core/common/pull/353)).
