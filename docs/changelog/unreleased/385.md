## zakura-halo2-proofs

### Changed

- Proving keys now retain the circuit configuration built by `keygen_pk` so
  `create_proof` does not configure the circuit again. Both functions now
  require the circuit's configuration type to be `'static`
  ([#385](https://github.com/zakura-core/common/pull/385)).
