<!-- changelog: none -->

This PR only changes private prover transcript serialization. It reuses each
scalar's canonical representation for both the Blake2b transcript state and
the proof writer, removing one field conversion per scalar. Transcript and
proof bytes and public and `pub(crate)` APIs are unchanged.
