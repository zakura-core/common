<!-- changelog: none -->

This PR only changes private permutation-prover buffer ownership. For the two
sparse permutation sets in each Orchard Action, it removes two allocation/free
pairs and 28,512 bytes of requested allocation and logical zero-fill traffic
(eight pairs and 114,048 bytes for four Actions), without changing public or
`pub(crate)` APIs, protocol arithmetic, or proof bytes.
