<!-- changelog: none -->

This PR only changes private Pasta-field lookup sorting. It partitions six
2,042-key sorts per Orchard Action by a 12-bit leading radix before sorting
within each radix. The implementation adds no heap or retained allocation and
uses 16 KiB of transient stack per active sort. Proof bytes and public and
`pub(crate)` APIs are unchanged.
