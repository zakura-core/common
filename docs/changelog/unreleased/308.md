<!-- changelog: none -->

This PR removes the `utilities::MulByInversePowerOfTwo` trait and its bound
before any stable release ships them (they appeared only in 1.1.0-rc.1), and
corrects the `1.1.0-rc.1` section of the zakura-halo2-gadgets changelog in
place so the assembled stable `1.1.0` notes never mention the trait. The
retained Pasta fast path is already recorded there by the amended #295 entry,
so there is no consumer-visible change left to record.
