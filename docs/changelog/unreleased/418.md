<!-- changelog: none -->

This PR only changes private proving-buffer ownership. It removes one 64 KiB
zero-filled allocation per lookup (192 KiB for one Orchard Action and 768 KiB
for four Actions) without changing public or `pub(crate)` APIs, protocol
arithmetic, or proof bytes.
