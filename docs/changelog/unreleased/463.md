<!-- changelog: none -->

This PR only changes private Blake2b transcript challenge handling. It returns
typed scalar challenges directly from the 64-byte hash output, removing one
canonical scalar encoding and decoding round trip for each of the 22 typed
prover challenges. Transcript and proof bytes and public and `pub(crate)` APIs
are unchanged.
