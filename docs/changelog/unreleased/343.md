## zakura-halo2-proofs

### Changed

- Reduced the first prepared one-Action Orchard proof by 0.281 ms on Apple M4
  with ten workers and 0.792 ms on AMD Linux with six workers by retaining the
  fixed upper-half generator tables used by the first IPA fold. At `k = 11`,
  explicit preparation now retains 1 MiB more data and adds 1.699 ms on M4 or
  5.119 ms on the Linux VM; proof bytes and public APIs are unchanged
  ([#343](https://github.com/zakura-core/common/pull/343)).
