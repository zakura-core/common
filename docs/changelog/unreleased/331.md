<!-- changelog: none -->

This PR pins the repository's development toolchain to Rust 1.97.1 and
switches the workspace to Cargo's MSRV-aware resolver. The published crates'
minimum supported Rust version (1.91), public APIs, dependencies, and behavior
are unchanged, so there is nothing for a crate consumer to see.
